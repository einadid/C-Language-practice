#include <stdio.h>
int main()
{
    int a;
    scanf("%d", &a);
    if (a > 0)
    {
        printf("Non Zero");
    }
    else if (a < 0)
    {
        printf("Non Zero");
    }
    else if (a == 0)
    {
        printf("Zero");
    }
    return 0;
}